syntax = "proto3";
package tasks;

service Task {
	rpc SendTask (TaskRequest) returns (TaskResponse);
}

message TaskRequest {
	uint32 num_tasks = 1;	
}

message TaskResponse {
	string file_name = 1;
	bool is_assigned = 2;
	bool is_map = 3;
	int32 tasknum = 4;
}
